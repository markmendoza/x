<template default>
  <main>
    class{
      construct(){
        this.data = [{name:"Mark"},{name:"Mendoza"}];
      }
    }
  </main>

  {if(this.data)}
    {for(var i in this.data)}
      <div>{(this.data[i].name)}</div>
    {/for}
  {else if(false)}
    <div>Wont show</div>
  {else}
    <div>Useful when rendering static contents</div>
  {/if}
</template>